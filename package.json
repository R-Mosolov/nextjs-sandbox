{
  "name": "nextjs-sandbox",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "clear": "rm -r .firebase out && rm .firebaserc firebase.json",
    "deploy": "clear && firebase login && firebase init && npm run build && npm run export && firebase deploy && npm run clear"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.3",
    "next": "10.0.5",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
